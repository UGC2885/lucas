import { Routes } from '@angular/router';
import { LoginComponent } from './pages/login/login.component';


// Aqui vai ser onde iremos colocar as rotas das paginas
export const routes: Routes = [
    {
    path: "login",
    component: LoginComponent
    }
];
